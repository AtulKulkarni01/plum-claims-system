fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
# --- dev / test ---
pytest>=8.0
httpx>=0.27
# --- optional: only needed for LLM-based extraction of raw/unstructured docs ---
# Extraction fails over Gemini (primary) -> OpenAI (fallback). Either/both/neither.
google-genai>=1.0
openai>=1.0
